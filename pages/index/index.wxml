<!--index.wxml-->
<view wx:if="{{hasImg===false}}" class="container">
  <view class="changshi button" bindtap="change">+</view>
</view>
<view wx:if="{{hasImg===true}}" class="container">
  <canvas style="width:{{annulusHeight}}px; height:{{annulusHeight}}px"canvas-id="myCanvas" />
</view>

